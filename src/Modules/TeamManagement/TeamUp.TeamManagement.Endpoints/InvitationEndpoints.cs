using TeamUp.Common.Endpoints;

namespace TeamUp.TeamManagement.Endpoints;

public sealed class InvitationEndpoints : IEndpointGroup
{
	public EndpointGroupBuilder MapEndpoints(EndpointGroupBuilder group)
	{
		return group;
	}
}
